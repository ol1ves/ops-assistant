# ========================================================
# WEB PACKAGE
# ========================================================
# Next.js app. Copy to .env.local in this directory.
# Next.js only loads env from the app root (packages/web), not the repo root.
#
# URL of the API server (must include protocol and port)
NEXT_PUBLIC_API_URL=http://localhost:8000
# API key for authenticating with the API server
NEXT_PUBLIC_API_KEY=demo-key-1
